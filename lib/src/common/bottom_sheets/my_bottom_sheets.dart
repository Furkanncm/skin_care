abstract class MyBottomSheets {
  /* static Future<void> showDashboardSelectBottomSheet(BuildContext context, {required DashboardBloc dashboardBloc}) {
    final id = UniqueKey().toString();
    return popupManager.showModalBottomSheet<void>(
      id: id,
      context: context,
      builder: (context) => DashboardSelectBottomSheet(id: id, dashboardBloc: dashboardBloc),
    );
  } */
}
